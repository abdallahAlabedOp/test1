import express from "express";

import {
  getUsers,
  addUser,
  getUser,
  deleteUser,
  updateUser,
} from "../controller/UserController.mjs";

const userRouter = express.Router();

userRouter.get("/users", async (req, res) => {
  console.log(typeofawait getUser()[0] ,"assssssssssssssssssssssssssssssssaasasdasdasvfdsafv asdv");
  const response = await getUser()[0];
  return res.send(response);
});

userRouter.get("/user/:id", async (req, res) => {
  return res.send(await getUsers(req.params.id));
});

userRouter.post("/user", addUser);

userRouter.delete("/delete_user/:id", deleteUser);

userRouter.put("/update_user/:id", updateUser);

export default userRouter;
